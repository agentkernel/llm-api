# 默认按文本处理并统一为 LF，保证跨平台（尤其 CI 上 git apply / bash）一致。
* text=auto eol=lf

# 补丁文件必须原样保留 LF，任何转换都会破坏 git apply
*.diff -text

# Shell 脚本必须 LF
*.sh text eol=lf

# Windows 脚本保留 CRLF
*.ps1 text eol=crlf

# 二进制资源不做转换
*.png binary
*.jpg binary
*.ico binary
*.asar binary
