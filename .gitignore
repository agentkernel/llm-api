node_modules/
dist/
out/
release/
coverage/
*.log
.env
.env.*
!.env.example
# Sub2API 官方克隆/私有分支（仅根目录那一份，含各自 .git），补丁以 sub2api-patch/ 纳管。
# 注意用前导斜杠锚定到仓库根，否则会误伤 companion/src/sub2api/ 等业务代码目录。
/sub2api/
# 本地自测运行时数据与密钥（含 pgdata、admin key、状态文件），禁止提交
.local/
# Sub2API 运行时生成的定价缓存（根目录那一份）
/data/
# 本地自测工具的编译产物
tools/miniredis-server/miniredis-server.exe
tools/**/go.sum
# local tooling
.DS_Store
Thumbs.db

# KC-Chat 运行时产物（插件自动生成，请勿入库）
.cursor/mcp.json
.cursor/kc-mcp/
.cursor/kc-mcp-messages/
.cursor/kc-chat-exports/
